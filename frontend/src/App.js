import React from 'react';

const App = () => {
    return (
        <div>
            <h1>Manga Generator Interface</h1>
            <input type="text" placeholder="Enter manga title" />
            <button>Generate</button>
        </div>
    );
};

export default App;